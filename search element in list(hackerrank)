#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int N,i,search,flag=0;
    scanf("%d",&N);
    int arr[N];
    for(i=0;i<N;i++){
        scanf("%d",&arr[i]);
    }
    scanf("%d",&search);
    for(i=0;i<N;i++){
        if(arr[i]==search){
            printf("Found");
            flag=1;
            break;
        }
    }
    if(!flag){
        printf("Not Found");
    }
    return 0;
}
