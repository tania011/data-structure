#include<stdio.h>
#include<limits.h>
#define MAX 100
#include<stdlib.h>


int main(){
 int n;
      scanf("%d",&n);
    int stack[n], top=-1;
    for(int i=0;i<n;i++){
      scanf("%d",&stack[++top]);
   }
        for(int i=0;i<2;i++){
       top--;
        }
  
    for(int i=0;i<4;i++){
       scanf("%d",&stack[++top]);
   }
    for(int i=0;i<1;i++){
       top--;
   }
      for(int i=0;i<2;i++){
       scanf("%d",&stack[++top]);;
   }
    for(int i=0;i<3;i++){
       top--;
   }
    for(int i=top;i>=0;i--){
      printf("%d ",stack[i]);
   }
        
    return 0;
        }
